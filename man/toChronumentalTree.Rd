% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimated_node_dates.R
\name{toChronumentalTree}
\alias{toChronumentalTree}
\title{Convert tree_and_sequences to a timetree using Chronumental}
\usage{
toChronumentalTree(
  tree_and_sequences,
  reference_strain = NA,
  date_column = "Collection_date",
  chronumental_path = NULL,
  n_steps = 10000,
  genome_size = NA,
  clock_filter_iqd = 0,
  clock = NA
)
}
\arguments{
\item{tree_and_sequences}{a list containing \code{tree} and \code{tree_tibble}}

\item{reference_strain}{name of the strain to use as date reference (t=0). NA -> earliest date}

\item{date_column}{the column of \code{sequences} containing the tip dates. A column of type \code{Date} is always full precision ("YYYY-MM-DD"); you must supply a character column of
\code{YYYY-MM-DD} / \code{YYYY-MM} / \code{YYYY} strings so chronumental can apply its
per-tip date error of 1 / 30 / 365 days.}

\item{chronumental_path}{path to chronumental binary}

\item{n_steps}{number of chronumental iterations}

\item{genome_size}{nt sequence length when branch lengths are in units of substitutions/site}

\item{clock_filter_iqd}{discard the dates of tips whose root-to-tip divergence sits more than
this many interquartile ranges from chronumental's regression line. 0, the default, leaves
its filter off. It masks those dates rather than dropping the tips, so a subclade whose
members are all filtered loses every date constraint and floats}

\item{clock}{molecular clock rate in substitutions/site/year, held fixed rather than fitted.
NA lets chronumental estimate it by Theil-Sen root-to-tip regression

Two types of node date are added: the collection date of the nearest tip, and the collection date of the nearest \emph{descendant} tip.}
}
\description{
Convert tree_and_sequences to a timetree using Chronumental
}
